name: "Check Terraform"

on:
  pull_request:
    types: [ opened, reopened, synchronize, ready_for_review ]
    branches: [ main ]

jobs:
  check:
    name: "Reusable Workflow"
    permissions:
      contents: read
      pull-requests: read
      id-token: write
      statuses: write
    uses: launchbynttdata/launch-workflows/.github/workflows/reusable-terraform-check.yml@088489d7b5f9ee3e0f63110b7fa5a13f7fd523e2
    with:
      auth_method: ${{ startsWith(github.event.repository.name, 'tf-aws') && 'aws' ||
        startsWith(github.event.repository.name, 'tf-az') && 'azure' || '' }}
    secrets: inherit # pragma: allowlist secret
