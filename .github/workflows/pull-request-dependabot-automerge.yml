name: Dependabot Auto-Merge

on:
  pull_request:
    types: [ opened, reopened, synchronize ]

jobs:
  dependabot-automerge:
    name: Dependabot Auto-Merge
    permissions:
      contents: write
      pull-requests: write
    uses: launchbynttdata/launch-workflows/.github/workflows/reusable-pr-dependabot-automerge.yml@088489d7b5f9ee3e0f63110b7fa5a13f7fd523e2
    secrets: inherit # pragma: allowlist secret
