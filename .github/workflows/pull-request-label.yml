name: Validate PR Title

on:
  pull_request:
    types: [ opened, reopened, edited, ready_for_review, synchronize ]

jobs:
  validate-title:
    name: Validate PR Title
    permissions:
      contents: read
      pull-requests: write
      statuses: write
    uses: launchbynttdata/launch-workflows/.github/workflows/reusable-pr-conventional-commit-title.yml@088489d7b5f9ee3e0f63110b7fa5a13f7fd523e2
